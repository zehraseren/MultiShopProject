@model GetByIdProductDetailDto

<div class="tab-pane fade show active" id="tab-pane-1">
    <h4 class="mb-3">
        @localizer.GetKey("Ürün Detayı", typeof(ProductDetailResources))
    </h4>
    <p>@Model.ProductDescription</p>
</div>